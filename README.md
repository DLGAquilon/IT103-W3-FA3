# IT103-W3-FA3
Made by Vin Belandres 
Composed of 2 machine problem set that uses HTML, CSS, and JavaScript

#Machine Problem 1: ReplaceAll
ReplaceAll is a JS practice activity in which the user provides spaced input and the program tries to replace it all with no spaces.
trim() function is not used.

#Machine Problem 3: ReplaceWord
ReplaceWord now uses a specific word to be replaced with another given word.
Regex are not used in this scenario
